# Backend API URL
# This environment variable is injected into the container at runtime
# Format: protocol://hostname:port (e.g., http://localhost:8000)
# For Docker: Use the backend service name (e.g., http://backend:8000)
# For local dev: Use localhost (e.g., http://localhost:8000)
BACKEND_URL=http://localhost:8004
